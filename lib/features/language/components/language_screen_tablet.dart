import 'package:flutter/material.dart';

class LanguageScreenTablet extends StatelessWidget {
  const LanguageScreenTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
